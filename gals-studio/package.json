{
  "name": "gals-studio",
  "version": "0.1.0",
  "private": true,
  "description": "GALS Studio — offline replay & coding studio for instrumented learning sessions.",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c green,cyan \"npm:dev:server\" \"npm:dev:web\"",
    "dev:server": "npm -w @gals-studio/server run dev",
    "dev:web": "npm -w @gals-studio/web run dev",
    "studio": "npm -w @gals-studio/web run build && npm -w @gals-studio/server run start",
    "db:migrate": "npm -w @gals-studio/server run db:migrate",
    "db:studio": "npm -w @gals-studio/server run db:studio",
    "build": "npm -w @gals-studio/shared run build && npm -w @gals-studio/web run build",
    "test": "npm -w @gals-studio/shared run test && npm -w @gals-studio/server run test",
    "typecheck": "npm -ws --if-present run typecheck"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
