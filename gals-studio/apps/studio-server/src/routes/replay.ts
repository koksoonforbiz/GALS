import type { FastifyInstance } from 'fastify';

// Filled in a later stage; registered now so the server bootstraps cleanly.
export async function replayRoutes(_app: FastifyInstance): Promise<void> {}
