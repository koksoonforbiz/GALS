# Socket.IO (all three gateways multiplex over this one path; the
# namespace is chosen in the handshake, which the API's Phase 4 gateway
# auth + door check governs). X-GALS-Door is set exactly like /api.
location /socket.io/ {
    proxy_pass http://gals_api;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-GALS-Door $gals_door;
    proxy_read_timeout 3600s;
    proxy_send_timeout 3600s;
}
