# Private-door access control — LOCAL default: no restriction. The admin
# port is published on 127.0.0.1 only (docker-compose.twodoor.yml), so
# nothing off the machine can reach it anyway.
#
# On the L40 host, bind-mount a file like admin-access.l40.example.conf in
# its place (GALS_ADMIN_ACCESS_FILE in deploy/.env.twodoor.l40).
