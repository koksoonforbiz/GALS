# Private-door access control — L40 EXAMPLE. Copy to a file outside git
# (or keep it here but never commit real ranges), then point
# GALS_ADMIN_ACCESS_FILE at it. Replace the placeholders with the campus /
# VPN ranges that staff actually come from; everything else gets a 403
# before the admin bundle or any API route is served.
#
# allow 203.0.113.0/24;      # example range (RFC 5737 documentation block)
# allow 198.51.100.0/24;     # example range
# deny  all;
