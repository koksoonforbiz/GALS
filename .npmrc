# Checklist item 19 — components sourced from trusted/reputable
# origins. Pins installs to the official npm registry explicitly,
# rather than relying on whatever registry an environment variable or
# an untracked local .npmrc happens to point at.
registry=https://registry.npmjs.org/
